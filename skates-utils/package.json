{
    "name": "skates-utils",
    "version": "0.2.0",
    "private": true,
    "description": "Utility package used in all skates-bundles.",
    "scripts": {
        "build": "tsc -b",
        "watch": "tsc -b -w",
        "clean": "tsc -b --clean"
    },
    "nodecg": {
        "compatibleRange": "^1.1.1"
    },
    "license": "MIT",
    "devDependencies": {
        "@types/node": "^14.14.33",
        "typescript": "^4.2.3"
    },
    "dependencies": {
        "nodecg": "^1.8.1",
        "nodecg-io-core": "^0.2.0"
    }
}
