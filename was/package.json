{
    "name": "was",
    "version": "0.2.0",
    "private": true,
    "nodecg": {
        "compatibleRange": "^1.1.1",
        "bundleDependencies": {
            "nodecg-io-twitch-chat": "^0.2.0"
        }
    },
    "scripts": {
        "build": "tsc -b",
        "watch": "tsc -b -w",
        "clean": "tsc -b --clean"
    },
    "license": "MIT",
    "devDependencies": {
        "@types/node": "^14.6.4",
        "@types/node-fetch": "^2.5.7"
    },
    "dependencies": {
        "node-fetch": "^2.6.1",
        "nodecg": "^1.8.1",
        "nodecg-io-core": "^0.2.0",
        "nodecg-io-twitch-chat": "^0.2.0",
        "typescript": "^4.0.2"
    }
}
